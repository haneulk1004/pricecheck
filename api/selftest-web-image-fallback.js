export default async function handler(req,res){try{const q='해태 갈아만든 배 340ml';const url='https://search.danawa.com/dsearch.php?query='+encodeURIComponent(q);const r=await fetch(url,{redirect:'follow',signal:AbortSignal.timeout(10000),headers:{'User-Agent':'Mozilla/5.0','Accept':'text/html,application/xhtml+xml'}});const html=await r.text();const needles=['갈아만든 배 340ml','갈아만든배 340ml','갈아만든 배'];let i=-1;for(const n of needles){i=html.indexOf(n);if(i>=0)break}const snippet=i>=0?html.slice(Math.max(0,i-2500),i+2500):html.slice(0,1000);return res.status(200).json({ok:r.ok,status:r.status,contentType:r.headers.get('content-type'),length:html.length,found:i>=0,snippet});}catch(e){return res.status(500).json({ok:false,error:e?.message||'failed'})}}
