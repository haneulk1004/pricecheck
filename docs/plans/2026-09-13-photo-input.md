# Photo input reliability

Preserve the existing 8 MiB original selection limit and product confirmation flow. Resize supported JPEG/PNG/WebP photos in-browser to at most 2000px, encode JPEG, and enforce a 4.1 MB JSON request ceiling. Do not add an external image store. Reject unsupported or unreadable files with Korean guidance. Bound provider and browser waits, sanitize errors, and disable Interactions retrieval storage. Validate oversized inputs and provider failures without real paid calls, then verify Preview rendering. Changes only on v100-ai-championship.
